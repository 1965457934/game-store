<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gamestore.mapper.OrderMapper">
    
    <resultMap id="orderMap" type="com.gamestore.entity.Order">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="pay_time" property="payTime"/>
        <result column="username" property="username"/>
        <collection property="items" ofType="com.gamestore.entity.OrderItem" 
                    select="selectOrderItems" column="id"/>
    </resultMap>
    
    <select id="selectOrderList" resultMap="orderMap">
        SELECT o.*, u.username 
        FROM order_info o 
        LEFT JOIN user u ON o.user_id = u.id 
        WHERE 1=1
        <if test="userId != null">
            AND o.user_id = #{userId}
        </if>
        <if test="status != null">
            AND o.status = #{status}
        </if>
        ORDER BY o.create_time DESC
    </select>
    
    <select id="selectOrderItems" resultType="com.gamestore.entity.OrderItem">
        SELECT oi.*, g.name as game_name, g.cover as game_cover
        FROM order_item oi
        LEFT JOIN game g ON oi.game_id = g.id
        WHERE oi.order_id = #{orderId}
    </select>
    
</mapper>
